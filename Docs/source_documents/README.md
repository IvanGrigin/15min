# Imported source documents

This directory stores unchanged source documents that have been added to the
repository but have not yet been incorporated into the indexed task corpus.

## Contract

- files here are read-only imports; preserve their original filename, encoding
  and contents;
- `Docs/all_tasks_all_files.md` remains the existing immutable master corpus
  and is not rewritten when a new source is imported;
- a future agent may create a separate index in `data/source_index/` for an
  imported document after its tasks have been reviewed and deduplicated.

## Current imports

- `Сборник задач по подготовке к 239 в 5 класс.txt` — UTF-8 source collection
  supplied by the user on 2026-07-15; its SHA-256 is
  `e22c567cdfca9b3dc3894919257f2b29c415d078e3ebef79838eaf2400d350cc`.
