# problemgen/generation/binding

Здесь должно происходить связывание шаблона с конкретными сущностями и словоформами.

## Примеры будущих функций

- `bind_entities_to_slots()`
- `bind_numbers_to_slots()`
- `build_slot_resolution_map()`
