# frontend

Здесь лежит браузерная часть локального интерфейса.

## Что должно быть в этой папке

- стили;
- клиентский JS;
- при необходимости маленькие вспомогательные ресурсы для UI.

`worksheet_site.js` нумерует все доступные модули в ручном selector в порядке,
который возвращает `/api/templates`; номер помогает соотнести пункт выбора с
модульным каталогом и не меняет его `module_id`.

`template_studio.js` — отдельный клиент локальной административной страницы:
он передаёт только JSON, показывает draft/preview/validation и вставляет
пользовательский текст через DOM-свойство `textContent`, а не как HTML.

`template_creator.js` — automatic-first форма одной задачи и modal с
provider-generated JSON, validation, preview, activation и deletion.

## Что не должно быть в этой папке

- доменная логика;
- словари сущностей;
- правила русского языка;
- генерация числовых параметров.
