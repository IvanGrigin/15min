# Делимость, кратные, остатки, простые

- Файлы: `data/templates/problem_sets/divisibility_multiples_remainders_primes/templates.json`,
  `problemgen/generation/divisibility_templates.py`.
- Ключевое: `_count(...)` для открытых/включённых промежутков; `math.lcm(...)`
  для составных признаков; неоднозначный №638 источника учтён как `active: false`.
- Проверка: `python scripts/validate_divisibility_templates.py` (3300 экз.),
  `tests/test_divisibility_templates.py`.
