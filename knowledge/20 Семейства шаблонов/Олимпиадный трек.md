# Олимпиадный трек (подготовка к 239)

- Каталог: `data/templates/olympiad_templates.json`; только модули/стратегии `olymp_*`.
- Гайд: `Docs/OLYMPIAD_AUTHORING.md`; генерация —
  `generate_problem_from_template(..., catalog_path="data/templates/olympiad_templates.json")`.
- Не смешивать с каталогом 15-минуток; текстовая логика —
  `problemgen/domains/olympiad_logic/` (`digit_erasing`, `birds_count`, ...).
