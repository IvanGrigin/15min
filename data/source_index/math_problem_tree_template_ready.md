# Template-Ready Math Problem Tree — moved

The production-planning row for each theme is now colocated with its taxonomy and corpus mapping in [`task_tree/`](task_tree/README.md).

This is planning metadata, not a statement that the current runtime generator implements the theme.
