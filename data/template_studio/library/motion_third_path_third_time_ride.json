{
  "schema_version": 1,
  "template_id": "motion_third_path_third_time_ride",
  "module_id": "motion_speed_and_distance",
  "language": "ru",
  "solver_strategy": "formula",
  "answer_type": "multi_part",
  "candidate_template_text": "{hero:nom} {hero:g,шёл|шла} первую треть пути со скоростью {v1} км/ч, затем ровно треть всего времени {hero:g,бежал|бежала} со скоростью {v2} км/ч, а остаток пути {hero:acc} {rider:g,вёз|везла} {rider:nom} со скоростью {v3} км/ч. Вся дорога заняла {hour:count,total_h}. 1) Чему равен весь путь? 2) Сколько километров {hero:nom} {hero:g,проехал|проехала}?",
  "parameter_schema": {
    "hero": {
      "type": "character"
    },
    "rider": {
      "type": "character"
    },
    "hour": {
      "type": "noun",
      "lemma": "час"
    },
    "v1": {
      "type": "integer",
      "min": 3,
      "max": 8
    },
    "v2": {
      "type": "integer",
      "min": 4,
      "max": 12
    },
    "walk_min": {
      "type": "choice",
      "allowed_values": [
        30,
        36,
        45
      ]
    },
    "run_min": {
      "type": "choice",
      "allowed_values": [
        20,
        30
      ]
    }
  },
  "derived_values": {
    "path_m": "50 * v1 * walk_min",
    "run_m": "v2 * run_min * 50 // 3",
    "ride_m": "2 * (50 * v1 * walk_min) // 3 - v2 * run_min * 50 // 3",
    "ride_min": "2 * run_min - walk_min",
    "total_min": "3 * run_min",
    "v3": "(2 * (50 * v1 * walk_min) // 3 - v2 * run_min * 50 // 3) * 3 // (50 * (2 * run_min - walk_min))",
    "path_km": "(50 * v1 * walk_min) / 1000",
    "ride_km": "(2 * (50 * v1 * walk_min) // 3 - v2 * run_min * 50 // 3) / 1000",
    "total_h": "(3 * run_min) / 60"
  },
  "constraints": [
    "v2 > v1",
    "(2 * path_m) % 3 == 0",
    "(v2 * run_min * 50) % 3 == 0",
    "ride_min >= 3",
    "ride_m > 0",
    "(ride_m * 3) % (50 * ride_min) == 0",
    "v3 > v2",
    "v3 <= 30",
    "path_m % 500 == 0",
    "ride_m % 100 == 0"
  ],
  "answer_expression": "[path_km, ride_km]",
  "answer_rendering": {
    "type": "multi_part",
    "parts": [
      {
        "label": "1)",
        "value": "path_km",
        "unit": "километр"
      },
      {
        "label": "2)",
        "value": "ride_km",
        "unit": "километр"
      }
    ]
  },
  "grammar_metadata": {
    "hero_role": "actor",
    "rider_role": "transport_helper",
    "notes": "Повышенный уровень: доля пути 1/3, бег быстрее ходьбы, два неизвестных."
  },
  "source_metadata": {
    "problem_number": "8",
    "filename": "усложнение задачи про хоббита"
  },
  "notes": "Повышенный уровень: треть пути и треть времени, спрашивается весь путь.",
  "math_notes": {
    "level": "advanced",
    "structure": "alpha=1/3, beta=1/3, скорость верхом известна, неизвестны весь путь и длина верхового участка",
    "math": [
      "path_m = 50*v1*walk_min (первая треть)",
      "run_m = v2*run_min*50/3, ride_m = 2*path_m/3 - run_m, ride_min = 2*run_min - walk_min",
      "v3 = ride_m*3/(50*ride_min)",
      "При alpha=1/3 допустимо v2 < 2*v1 — здесь бег уже может быть быстрее ходьбы."
    ],
    "quality": "v3 > v2: верхом быстрее, чем бегом."
  }
}
