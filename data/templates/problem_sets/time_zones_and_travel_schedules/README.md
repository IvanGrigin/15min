# Часовые пояса и расписания поездок

`templates.json` содержит active integer-шаблоны модуля 17, а
`source_accounting.json` — все 43 deduplicated-источника. Проверка:
`py -3 scripts/validate_time_zone_templates.py`.
