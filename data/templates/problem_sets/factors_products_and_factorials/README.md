# Множители, произведения и факториалы

Набор объединяет 31 уникальную задачу модуля 09. Runtime-каталог
`templates.json` содержит только семь exact-integer семейств, а
`source_accounting.json` учитывает каждый источник ровно один раз и хранит
причины исключения конструктивных, списочных и логических ответов.

Генератор: `problemgen/generation/factor_product_templates.py`.
Проверка: `python scripts/validate_factor_product_templates.py`.
