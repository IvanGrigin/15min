# Движение, скорость и расстояние

Набор модуля 18: active integer-шаблоны в `templates.json`, полный учёт
60 deduplicated-источников в `source_accounting.json`. Проверка:
`py -3 scripts/validate_motion_templates.py`.
